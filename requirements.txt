# Runtime dependencies for the new src/ rewrite.
#
# Install these into the workspace-local .venv from D:\code\sam3.
# Install PyTorch separately with the CUDA wheel/index that matches the machine.
# Example: install torch and torchvision separately with the CUDA wheel index
# that matches the machine.

numpy
pillow
tqdm
typing_extensions
iopath
timm
einops
opencv-python
pytest
